# 🚀 Инструкция по запуску BroLearn

## Предварительные требования

- **Node.js** 18+ и npm
- **MongoDB** (локально или Docker)
- **Expo CLI**: `npm install -g @expo/cli`

## 📦 Установка зависимостей

### Backend

```bash
cd backend
npm install
```

### Frontend

```bash
npm install
```

## 🗄️ Настройка базы данных

### Запуск MongoDB (Docker)

```bash
docker run -d -p 27017:27017 --name mongodb mongo:latest
```

### Создание .env файла

Создайте файл `backend/.env`:

```env
PORT=3000
MONGODB_URI=mongodb://localhost:27017/bro-learn
JWT_SECRET=your-super-secret-key-change-in-production
JWT_REFRESH_SECRET=your-refresh-secret-key-change-in-production
JWT_EXPIRES_IN=7d
JWT_REFRESH_EXPIRES_IN=30d
NODE_ENV=development
```

### Заполнение базы данных

```bash
cd backend
npm run seed
```

## 🎯 Запуск приложения

### 1. Запуск Backend сервера

```bash
cd backend
npm run dev
```

Сервер запустится на `http://localhost:3000`

### 2. Запуск Frontend приложения

В новом терминале:

```bash
npm start
```

Затем выберите платформу:
- `w` - веб-версия
- `a` - Android
- `i` - iOS (только на macOS)

## 📱 Тестирование на устройстве

1. Установите **Expo Go** из App Store / Google Play
2. Отсканируйте QR-код из терминала
3. Приложение загрузится автоматически

## 🔧 Полезные команды

### Backend

```bash
npm run dev      # Запуск в режиме разработки
npm run build    # Сборка проекта
npm start        # Запуск production версии
npm run seed     # Заполнение БД тестовыми данными
```

### Frontend

```bash
npm start        # Запуск Expo dev server
npm run android  # Запуск на Android
npm run ios      # Запуск на iOS
npm run web      # Запуск веб-версии
npm run lint     # Проверка кода
```

## 📊 Структура проекта

```
bro-learn/
├── backend/              # Express.js API
│   ├── src/
│   │   ├── models/      # Mongoose модели
│   │   ├── controllers/ # Контроллеры API
│   │   ├── routes/      # Роуты API
│   │   ├── middleware/  # Middleware
│   │   └── scripts/     # Скрипты (seed и т.д.)
│   └── package.json
├── app/                 # React Native экраны
│   ├── (tabs)/         # Табы навигации
│   ├── auth/           # Экраны авторизации
│   ├── course/         # Экраны курсов
│   └── lesson/         # Экраны уроков
├── components/          # React компоненты
├── services/           # API сервисы
├── stores/             # Zustand stores
└── constants/          # Константы и темы
```

## 🎨 Особенности

- ✅ JWT авторизация
- ✅ 10 курсов обучения
- ✅ Система XP и уровней
- ✅ Серия дней (streak)
- ✅ Система достижений
- ✅ Квизы и практические задания
- ✅ Темная/светлая тема
- ✅ Прогресс по курсам

## 🐛 Возможные проблемы

### MongoDB не запускается

```bash
# Проверьте статус контейнера
docker ps -a

# Перезапустите контейнер
docker restart mongodb
```

### Ошибка подключения к API

Убедитесь, что:
1. Backend сервер запущен
2. В `services/api.ts` правильный URL (для устройства используйте IP компьютера вместо localhost)

### Expo не открывается

```bash
# Очистите кэш
npm run reset-project
```

## 📝 Тестовые данные

После запуска `npm run seed` в базе будут созданы:
- 10 курсов с уроками
- 9 достижений
- Различные типы уроков (теория, квизы, практика)

Для тестирования создайте нового пользователя через экран регистрации.

## 🎓 Курсы в приложении

1. GigaChat
2. DeepSeek
3. Perplexity
4. Браузер Comet
5. AI Агенты
6. Skills для агентов
7. MCP Серверы
8. RAG системы
9. Локальные LLM
10. Cursor.AI продвинутый

Приятного обучения! 🚀
